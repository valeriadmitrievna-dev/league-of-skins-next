"use client";

import { ScrollArea } from "@/components/ui/scroll-area";

const AdministrationTest = () => {
  return (
    <ScrollArea className="size-full bg-muted/50 overflow-hidden rounded-md p-2 flex flex-col gap-y-2 font-mono text-sm"></ScrollArea>
  );
};

export default AdministrationTest;
